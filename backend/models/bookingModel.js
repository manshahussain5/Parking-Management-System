// Booking model (for reference, since we're using a JSON DB)
// Bookings will be stored in db.json

/*
{
  id: string,
  userId: string,
  lotId: string,
  slotId: string,
  date: string (YYYY-MM-DD),
  time: string (HH:mm),
  status: 'active' | 'cancelled' | 'completed',
  createdAt: ISOString
}
*/

// No code needed here for JSON DB, but this file documents the structure.
